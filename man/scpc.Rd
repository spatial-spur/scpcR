% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scpcR.R
\name{scpc}
\alias{scpc}
\title{Spatial Correlation-Robust Inference (SCPC)}
\usage{
scpc(
  model,
  data,
  lon = NULL,
  lat = NULL,
  coord_euclidean = NULL,
  cluster = NULL,
  k = 10,
  avc = 0.03,
  uncond = FALSE,
  cvs = FALSE
)
}
\arguments{
\item{model}{Fitted model object. Currently supported classes are \code{lm}
and \code{fixest}.}

\item{data}{Data frame used to fit \code{model}.}

\item{lon}{Optional column name for longitude. Must be supplied together with
\code{lat}.}

\item{lat}{Optional column name for latitude. Must be supplied together with
\code{lon}.}

\item{coord_euclidean}{Optional character vector of one or more column names
for Euclidean coordinates. Supply either \code{lon}/\code{lat} or
\code{coord_euclidean}, not both.}

\item{cluster}{Optional clustering variable. Supply either a vector aligned to
the model estimation sample or a full-length vector aligned to \code{data}.}

\item{k}{Number of coefficients to report.}

\item{avc}{Upper bound on average pairwise correlation (must be in
\code{(0.001, 0.99)}).}

\item{uncond}{Logical; if \code{TRUE}, use unconditional critical values only.}

\item{cvs}{Logical; if \code{TRUE}, include per-coefficient critical values at
32\%, 10\%, 5\%, and 1\%.}
}
\value{
An object of class \code{scpc} with components:
\itemize{
\item \code{scpcstats}: matrix of coefficient estimates, SCPC standard errors,
test statistics, p-values, and confidence interval bounds.
\item \code{scpccvs}: optional matrix of critical values when \code{cvs = TRUE}.
\item \code{W}, \code{avc}, \code{c0}, \code{cv}, \code{q}, \code{call}.
}
}
\description{
Compute spatial correlation-robust inference for regression coefficients.
}
